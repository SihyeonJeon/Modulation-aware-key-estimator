# Security

Do not commit browser cookies, downloaded audio, API keys, or private model
artifacts.

YouTube sources that require authentication can be used by setting
`YTDLP_COOKIES_PATH=/path/to/cookies.txt` locally. The cookies file should stay
outside the repository.

Report security issues privately through GitHub if the repository is public.

